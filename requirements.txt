# ── Core framework ───────────────────────────────────────────
fastapi
uvicorn
python-multipart
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# ── Deep learning ────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0
timm>=0.9.16
transformers>=4.40.0
accelerate>=0.28.0

# ── Face detection ───────────────────────────────────────────
# Note: facenet-pytorch must be installed with --no-deps under Python 3.14 to prevent Pillow/NumPy backtracking conflicts.
# facenet-pytorch
efficientnet-pytorch
opencv-python>=4.9.0

# ── Audio ────────────────────────────────────────────────────
openai-whisper>=20231117
ffmpeg-python>=0.2.0
librosa>=0.10.0
soundfile>=0.12.1

# ── NLP / fact-checking ──────────────────────────────────────
spacy>=3.7.0
sentence-transformers>=2.7.0
duckduckgo-search>=5.3.0
newsapi-python>=0.2.7
python-dotenv>=1.0.1

# ── Metadata forensics ───────────────────────────────────────
exifread>=3.0.0
Pillow>=10.0.0

# ── ML utilities ─────────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas
tqdm

# ── HTTP ─────────────────────────────────────────────────────
requests>=2.31.0

# ── TensorBoard (training) ───────────────────────────────────
tensorboard>=2.16.0
